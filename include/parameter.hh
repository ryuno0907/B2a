#ifndef parameter_h
#define parameter_h 1

double p_trackerWidth = 3.7; // width of the chambers
double p_targetWidth = 2.5; // width of the target

double p_targetLength = 0.5; // full length of Target

double p_trackerLength = 2;

double p_trackerRadius  = p_trackerWidth/2;   // Radius of Target
double p_targetRadius  = p_targetWidth/2;   // Radius of Target
double p_collimatorDz = 0.2;

#endif